feat: real Pharmacy pages, real Doctor RPM/ERx, build code-splitting

Pharmacy portal
- pharmacy/Shipping: live shipping queue from orders (status in shipped/delivered),
  carrier deep links (UPS/FedEx/USPS), Mark as Delivered, label print, search
- pharmacy/Inventory: SKU CRUD against pharmacy_inventory with low-stock badge,
  reorder threshold tracking, total inventory value, realtime channel
- pharmacy/Settings: dispensary credentials (NPI/DEA/state license) and
  integration prefs (carrier, SureScripts), upserts to pharmacy_settings

Doctor portal
- doctor/RPM: live vital_readings via realtime channel, per-patient BP series,
  critical alerts queue, computed stats (active devices, alerts, stable %, syncs)
- doctor/ERx: real prescription send (insert into prescriptions), patient
  picker from orders, formulary search, drug-interaction cross-check against
  patient's current medication

Build
- vite.config: manualChunks splits react/recharts/supabase/framer-motion/
  lucide/router/ui/state into separate vendor chunks; main bundle drops
  from 1.65 MB to 588 KB

Migrations
- supabase_vital_readings.sql, supabase_pharmacy_inventory.sql,
  supabase_pharmacy_settings.sql with RLS policies
