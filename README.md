
  # Build with Tokens

  This is a code bundle for Build with Tokens. The original project is available at https://www.figma.com/design/THzX8hl7t7xJjdoUZQ0pRs/Build-with-Tokens.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  