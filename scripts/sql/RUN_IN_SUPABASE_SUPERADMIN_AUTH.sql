-- =============================================================================
-- DEPRECATED — use RUN_IN_SUPABASE_STAFF_AUTH_ALL.sql instead
-- =============================================================================
-- Paste and run: scripts/sql/RUN_IN_SUPABASE_STAFF_AUTH_ALL.sql
-- It provisions ALL staff portals (doctor, admin, super admin, pharmacy, affiliate).
-- =============================================================================

SELECT 'Use RUN_IN_SUPABASE_STAFF_AUTH_ALL.sql — this file is deprecated' AS notice;
